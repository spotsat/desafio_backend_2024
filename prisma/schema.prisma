datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Graph {
  id        Int      @id @default(autoincrement())
  // Outros campos como coordenadas, etc.
}
